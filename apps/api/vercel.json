{
  "version": 2,
  "installCommand": "cd ../.. && npm install && cd apps/api && npm install && prisma generate --schema=./prisma/schema.prisma",
  "buildCommand": "npm run build",
  "framework": null,
  "builds": [
    {
      "src": "index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/index.ts"
    }
  ]
}
